@model Product


<span class="post-container-mini">
	@*@{
			string[] imageSources = new string[Model.Images.Length];
			for (int i = 0; i < Model.Images.Length; i++)
			{
				string base64 = Convert.ToBase64String(Model.Images[i]);
				imageSources[i] = String.Format("data:image/gif;base64,{0}", base64);
			}
		}

		<span class="image-container">
			@foreach (string source in imageSources)
			{
				<img src="@source" />
			}
		</span>*@
	<span class="title-container">
		@Model.Title
	</span>
	<span class="DetailsContainer">
		Posted by @Model.SellerId at @Model.Date
	</span>
	<span class="price-container">
		@Model.Price
	</span>
	<span class="short-description-container">
		@Model.ShortDescription
	</span>
</span>