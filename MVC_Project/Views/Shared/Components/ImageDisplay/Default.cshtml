@model Image

@{
	if (Model != null)
	{
		string base64 = Convert.ToBase64String(Model.ByteArray);
		<img src=@String.Format("data:image/jpg;base64,{0}", base64) />
	}
	else
	{
		<img src="~/images/no_image.png" alt="no-image">
	}
}